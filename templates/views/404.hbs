<html>
    <head>
        {{>stylesheets}}
        <link rel="icon" href="/images/download.png">
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{error}}</p>

        </div>
        {{>footer}}
    </body>
</html>